$ kafka-sever-start.sh -daemon config/server.properties

Find current program:
jps

Vim:
i  --insert
:q!  --quit without save
:wq  --save and quit

$ kafka-topics.sh --create --zookeeper
$ kafka-topics.sh --list --zookeeper
$ kafka-topics.sh --delete --zookeeper --topic first
$ kafka-topics.sh --describe --topic first
